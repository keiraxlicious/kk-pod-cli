apiVersion: apps/v1
kind: Deployment
metadata:
  name: assembly-dev
  namespace: kk-dev
  labels:
    app: assembly-dev
    tier: dev-environment
    lang: assembly
spec:
  replicas: 1
  selector:
    matchLabels:
      app: assembly-dev
  template:
    metadata:
      labels:
        app: assembly-dev
        tier: dev-environment
    spec:
      containers:
        - name: assembly
          image: ubuntu:24.04
          command: ["/bin/bash", "-c", "apt-get update && apt-get install -y nasm && sleep infinity"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: shared-data
              mountPath: /shared
      volumes:
        - name: workspace
          hostPath:
            path: /tmp/kk-workspace
            type: DirectoryOrCreate
        - name: shared-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: assembly-dev
  namespace: kk-dev
spec:
  selector:
    app: assembly-dev
  ports:
    - name: dummy
      port: 9999
      targetPort: 9999
