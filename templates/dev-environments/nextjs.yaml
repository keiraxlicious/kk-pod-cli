apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextjs-dev
  namespace: kk-dev
  labels:
    app: nextjs-dev
    tier: dev-environment
    lang: nextjs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nextjs-dev
  template:
    metadata:
      labels:
        app: nextjs-dev
        tier: dev-environment
    spec:
      containers:
        - name: nextjs
          image: node:22-slim
          command: ["sleep", "infinity"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "2000m"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: shared-data
              mountPath: /shared
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "development"
      volumes:
        - name: workspace
          hostPath:
            path: /tmp/kk-workspace
            type: DirectoryOrCreate
        - name: shared-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: nextjs-dev
  namespace: kk-dev
spec:
  type: NodePort
  selector:
    app: nextjs-dev
  ports:
    - name: dev-server
      port: 3000
      targetPort: 3000
      nodePort: 30305
