apiVersion: apps/v1
kind: Deployment
metadata:
  name: prolog-dev
  namespace: kk-dev
  labels:
    app: prolog-dev
    tier: dev-environment
    lang: prolog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prolog-dev
  template:
    metadata:
      labels:
        app: prolog-dev
        tier: dev-environment
    spec:
      containers:
        - name: prolog
          image: swipl:9.2
          command: ["sleep", "infinity"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: shared-data
              mountPath: /shared
      volumes:
        - name: workspace
          hostPath:
            path: /tmp/kk-workspace
            type: DirectoryOrCreate
        - name: shared-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: prolog-dev
  namespace: kk-dev
spec:
  selector:
    app: prolog-dev
  ports:
    - name: dummy
      port: 9999
      targetPort: 9999
