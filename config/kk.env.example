# KiraKota Fill Worker Configuration
# Copy to ~/.config/kk/kk.env and edit for your system.
# On WSL2, paths are auto-detected — this file is only needed on native Linux.

# --- Course Paths ---
# Where your Mastery-Courses and Skills directories live
KK_COURSES_DIR=/home/user/Mastery-Courses
KK_SKILLS_DIR=/home/user/Skills

# --- Container Images ---
# Override the default worker image (kk/claude-workspace:latest)
# KK_IMAGE_WORKER=docker.io/kk/claude-workspace:latest
# KK_IMAGE_PULL_POLICY=Never       # Never (local) | Always | IfNotPresent

# Optional registry prefix for remote images
# KK_IMAGE_REGISTRY=ghcr.io/youruser

# --- Auth ---
# Claude OAuth is used by default (tokens in ~/.claude/).
# Set this only if using API key auth instead:
# ANTHROPIC_API_KEY=sk-ant-...
